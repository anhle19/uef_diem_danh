@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="utf-8" />
    <title>In Danh Sách Học Viên</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <!-- (Tuỳ chọn) Bootstrap chỉ để dùng vài class tiện lợi -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Bootstrap Icon -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.13.1/font/bootstrap-icons.min.css">

    <!-- Axios -->
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>

    <!-- Moment JS -->
    <script src="https://cdn.jsdelivr.net/npm/moment@2.30.1/moment.min.js"></script>

    <!-- Global Config Variable -->
    <script type="text/javascript" src="/js/Configs.js"></script>

    <style>
        /* ========= Mặc định màn hình ========= */
        :root {
            /* Quy đổi 204x321 px ~ 54x85 mm (96dpi) để in ra đúng kích thước */
            --card-w-mm: 40mm;
            --card-h-mm: 60mm;
            --gap-mm: 3mm;
            --pad-x-mm: 7.5mm; /* 2 bên: 7.5 + 7.5 = 15mm, vừa khít 5 cột */
            --pad-y-mm: 12.5mm; /* trên + dưới, canh giữa dọc trang */
        }

        html, body {
            height: 100%;
        }

        body {
            height: auto;
            background: #f8f9fa;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
        }

        /* Tờ A4 nằm ngang: 297 x 210 mm */
        .sheet {
            background: #fff;
            box-sizing: border-box;
            margin-top: 30px;
            margin-bottom: 30px;
            display: grid;
            grid-template-columns: repeat(4, var(--card-w-mm));
            gap: var(--gap-mm);
        }

        /* Card theo đúng kích thước yêu cầu */
        .student-card {
            width: var(--card-w-mm);
            height: var(--card-h-mm);
            border-radius: 15px;
            box-shadow: 0 4px 8px rgba(0,0,0,.10);
            overflow: hidden;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
            border: 2px solid #e9ecef;
        }

        /* Khu trên chiếm vùng trung gian (để barcode bám đáy) */
        .card-top {
            flex: 1;
            min-height: 0;
            display: flex;
            flex-direction: column;
        }

        /* Header logo + tên trung tâm */
        .card-header-center {
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 8px 8px 0 8px;
            text-align: center;
        }

        .center-title {
            margin: 0;
            font-size: 8px;
            line-height: 1.2; /* tránh cắt dấu tiếng Việt */
            word-break: break-word;
        }

        .logo {
            width: 25px;
            height: 25px;
            flex: 0 0 auto;
        }

        .card-img-top {
            width: 80px;
            height: 80px;
            object-fit: cover;
            border-radius: 10px;
            border: 4px solid #ffffff;
            display: block;
            margin: 0 auto 6px;
        }

        .card-body {
            padding: 1px 6px 8px 10px;
        }

        .card-body h5 {
            font-size: 0.75rem; /* vừa mắt trên card 54x85mm */
            font-weight: 700;
            margin-bottom: 4px;
            color: #0d6efd;
        }

        .card-body p {
            font-size: 0.55rem;
            margin: 0 0 4px 0;
            color: #6c757d;
        }

        .barcode-section {
            padding: 5px 0 7px;
            background: #ffffff;
            border-top: 1px solid #e9ecef;
            text-align: center;
        }

        .print-btn-container {
            position: fixed;
            top: 10px;
            right: 10px;
        }

        .print-btn {
            background-color: #0d6efd;
            border: 0;
            border-radius: 50%;
            transition: background-color 0.15s ease;
        }

        .print-btn:hover {
            background-color: #0b55d4;
        }

    </style>
</head>
<body>

    <!-- STUDY CLASS ID HIDDEN INPUT -->
    <input type="hidden" name="studyClassIdInput" id="studyClassIdInput" value="@ViewBag.StudyClassId" />

    <div id="generateCardsArea">
    </div>
   
    <div id="generateCardsResultArea">
    </div>




    <div id="print-btn-container" class="print-btn-container">
        <button class="print-btn px-3 py-2"
                type="button"
                onclick="openPrintDialog()">
            <i class="bi bi-printer-fill" style="color: white; font-size: x-large;"></i>
        </button>
        <button class="print-btn px-3 py-2"
                type="button"
                onclick="downloadStudentCards('@ViewBag.StudyClassId')">
            <i class="bi bi-download" style="color: white; font-size: x-large;"></i>
        </button>
    </div>

    <!-- JsBarcode -->
    <script src="https://cdn.jsdelivr.net/npm/jsbarcode@3.11.5/dist/JsBarcode.all.min.js"></script>

    <script src="~/js/StudentCardMultiplePrint.js"></script>
</body>
</html>
